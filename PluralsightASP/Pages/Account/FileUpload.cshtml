@page
@model PluralsightASP.Pages.Account.FileUpload

@{
}

<main class="page">
    @foreach (var file in Model.Files)
    {
        <div>@file</div>
    }
    
    @*<section class="clean-block clean-form dark">

        <div class="container">
            <form method="post" enctype="multipart/form-data" class="form-control-file">

                <div class="form-group row custom-file">
                    
                    <input id="file" name="file" asp-for="File" class="custom-file-input" type="file">
                    <label for="file" class="custom-file-label">File</label>
                    
                </div>
                <div class="form-group row">
                    <div class="offset-4 col-12">
                        <button name="submit" type="submit" class="btn btn-primary">Upload file</button>
                    </div>
                </div>
                @if (!string.IsNullOrEmpty(Model.FileName))
                {
                    <span>
                        <br/>
                        <img src="~/images/@Model.FileName" class="img-fluid"/>
                    </span>
                }


            </form>
        </div>
    </section>*@
</main>