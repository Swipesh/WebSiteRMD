@page
@model IndexModel

@{
    ViewData["Title"] = "Scientific projects";
}
<div class="container">
    <div class="block-heading">
        <h2 class="text-info" localize-content>@ViewData["Title"]</h2>
    </div>
    <div class="container col-auto mt-2">
        <div class="row">
            <partial name="_ProjectCardPartial"/>
            <partial name="_ProjectCardPartial"/>
            <partial name="_ProjectCardPartial"/>
        </div>
    </div>
</div>